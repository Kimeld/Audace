<?php

/*
$query = $pdo->query('SELECT * FROM expenses');
$test = $query->fetchAll();  
$tab = [];
$tab[] = $test;
//tab of data


$nameSpent = $_GET['nameSpent'];
$cost = $_GET['cost'];

if(!empty($_GET))
{
   $prepare = $pdo->prepare('INSERT INTO expenses (name, amount) VALUES (:name, :amount)');


$prepare->bindValue(':name', $_GET['nameSpent']);
$prepare->bindValue(':amount', $_GET['cost']);



$execute = $prepare->execute();

//execution of the request

 
    
    
}*/

